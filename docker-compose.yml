version: '3.4'
services:
    proxxy:
        container_name: proxxy
        image: proxxy # name image for datadog tracing
        build:
            context: .
            dockerfile: ./docker/Dockerfile
        env_file:
            - .env
        volumes:
            - ./:/go/cmd/app
